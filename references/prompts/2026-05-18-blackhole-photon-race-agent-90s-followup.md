请继续当前任务。不要再反复尝试把用户上传图片写入 image_path，也不要因为参考素材路径问题停止。

当前状态已经满足关键要求：
- Trio.png 已确认读取成功，三主角形象清晰。
- 已经生成 9 个分镜、总时长约 98 秒的 storyboard。
- 上传素材已经存在：图片1 LightMind 眼镜，图片2 啦啦侠服装，图片3 飒飒君服装，图片4 Trio.png，图片5 手工拼皮笔记本。

请直接使用这些已上传素材作为视觉参考继续生成视频，不需要重新生成角色参考素材，不需要再修复 image_path。保持 storyboard 的 9 个分镜和 90 秒左右总时长，16:9 横屏，普通可用模型，不使用 VIP 模型。

请现在开始生成 9 个分镜视频，并在所有分镜成功后合成最终完整视频。不要输出 13 秒或 15 秒短片；如果某个分镜失败，请重试失败分镜，不要把局部片段合成短片。不要字幕。
